# azure.yaml
# Azure Developer CLI (azd) configuration for Shade - Cloud-Native Utility Platform

name: shade-platform
metadata:
  template: shade-platform@1.0.0-beta

# Configure the Azure resources
infra:
  provider: bicep
  path: ./infra
  resourceGroupName: "rg-shade-dev-eus-01"
